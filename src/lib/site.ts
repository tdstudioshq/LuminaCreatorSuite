/**
 * Canonical public site constants. Single source of truth for the production
 * domain used in shareable creator links and display prefixes, so a domain
 * change is a one-line edit rather than scattered literals.
 */
export const PUBLIC_SITE_DOMAIN = "cabanagrp.com";
