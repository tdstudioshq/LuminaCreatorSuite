# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Start here

- **Before new work:** read `docs/CLAUDE_SESSION_HANDOFF.md` — it holds the live phase state and the next recommended task. `CABANA_ARCHITECTURE.md` and `docs/CABANA_BUILD_ROADMAP.md` are the other source-of-truth docs.
- **Handoff gate (must pass before ending a session):** `bun run lint`, `bun run build`, `bunx tsc --noEmit`, `bun run test`. End the session by updating the handoff doc.
- **Load-bearing invariants** (details below): money is **demo-only** (integer cents, `mock_*` refs — no Stripe/payouts/KYC); deploy target is **Vercel, not Cloudflare**; new domain logic goes in a **pure, repository-injected module** under `src/lib/cabana-*.ts` (95% coverage gate); schema changes need an **approved migration + RLS + behavioral test**; never hand-edit `src/routeTree.gen.ts` or the Lovable-generated Supabase clients; keep private/member data off public routes.
- **Orientation map:** routing/SSR + Supabase client tiers → **Architecture**; the live subscription-platform phases (posts, subs, messaging, ledger, notifications, moderation, finance, discovery, analytics) → **Subscription-platform conventions**; the non-negotiables → **Hard constraints**.

## Project

CABANA is a premium creator OS / link-in-bio / storefront app being evolved into a creator subscription platform. The product name in code and UI is **CABANA** (the repo folder is `LuminaCreatorSuite`). It runs on TanStack Start (SSR) + React 19 + Vite 7 + Tailwind CSS 4, **deployed to Vercel** (production: `cabanagrp.com`) via Nitro's `vercel` preset, with Supabase for auth, data, and storage. The **canonical Supabase backend is the cloud project `rpzaeqoqcaxxavltgvpe` ("cabanadatabase")** — reconciled to the CABANA schema July 2026 via the scripts in `supabase/reconcile/`; the local Docker stack is for validating migrations before they go to cloud (never `db push` to cloud without explicit approval).

The source-of-truth planning docs are `CABANA_ARCHITECTURE.md`, `docs/CABANA_BUILD_ROADMAP.md`, and `docs/CLAUDE_SESSION_HANDOFF.md`. Read the handoff before starting new work — it tracks phase state and the next recommended task. `docs/` also carries reference docs (`CABANA_PROJECT_STATE.md`, `CABANA_ROUTE_MAP.md`, `CABANA_COMPONENT_MAP.md`, `CABANA_DATABASE.md`, `CABANA_API.md`, `CABANA_BUILD_PHASES.md`, `CABANA_PRODUCT_SPEC.md`, `CABANA_TECH_DEBT.md`) — keep whichever ones your change touches in sync. **End every session by producing an updated handoff** (files changed, routes/components built, whether Supabase schema/data was touched, demo vs. production behavior, and lint/build/tsc results).

`GEMINI.md` at the repo root is a parallel, higher-level restatement of this same architecture for a different agent — when a change here corrects load-bearing architecture (deploy target, client boundaries, the pure-layer/test model, monetization constraints), update `GEMINI.md` to match so the two don't drift.

## Commands

This project uses **Bun** for local dev and CI. If `bun` is not on PATH, prefix with `export PATH="$HOME/.bun/bin:$PATH"`. **Two lockfiles are maintained on purpose:** `bun.lock` (dev/CI installs) and `pnpm-lock.yaml` (Vercel builds; `.npmrc` sets `node-linker=hoisted` so Vercel resolves transitive deps) — when adding a dependency, update both.

```bash
bun run dev            # Vite dev server
bun run build          # Production client + SSR build
bun run build:dev      # Same build, mode=development (unminified — for debugging the build output)
bun run lint           # ESLint (Prettier runs as a lint rule — formatting issues are lint errors)
bun run format         # prettier --write . (fixes the formatting lint errors)
bunx tsc --noEmit      # Typecheck (tsconfig has noEmit; this is the only type check)

bun run test           # vitest run (one-shot)
bun run test:watch     # vitest watch mode
bun run test:coverage  # vitest with v8 coverage (thresholds enforced — see below)
bunx vitest run src/lib/cabana-money.test.ts   # run a single test file
bunx vitest run -t "rounds half up"            # run tests matching a name

bun run db:reset       # supabase db reset (rebuild local DB from migrations + seed)
bun run db:validate    # scripts/db-validate.sh — rebuilds a fresh Supabase from zero, runs smoke checks
bun run smoke:prod     # scripts/smoke-prod.ts — post-deploy production smoke test (see "Post-deploy verification")
```

⚠️ **NEVER run `supabase db push` against cloud** — the cloud `schema_migrations` ledger holds 22 reconcile-era versions, not this repo's `202605xx` numbers, so push would attempt to re-apply already-applied migrations. Apply cloud SQL via the Management API pattern only (see the July 9–10 handoff), until a dedicated migration-history repair session reconciles it.

**Tests** use **vitest** (`vitest.config.ts`, separate from `vite.config.ts`). The suite has three tiers, and only the first is gated:

1. **Pure business layer — the 95% coverage gate.** `cabana-money`, `cabana-entitlements`, `cabana-account`, `cabana-relationships`, `cabana-posts`, `cabana-engagement`, `cabana-subscriptions`, `cabana-messaging`, `cabana-notifications`, `cabana-moderation`, `cabana-finance`, `cabana-payouts`, `cabana-notification-engine`, `cabana-discovery`, `cabana-dashboard`, `cabana-creator-analytics`, `cabana-redirect`, `cabana-stream`, `cabana-stream-upload`, `cabana-admin-creators`, `cabana-creator-pages`, `cabana-admin-roles`, `cabana-creator-page-view`, `cabana-admin-creator-page-detail`, `cabana-admin-creator-editor`, `cabana-composer-media`. Thresholds are **95%** lines/functions/branches/statements over **exactly** the files in `coverage.include` — that array in `vitest.config.ts` is the source of truth, not this list. Keep new domain logic in a pure, repository-injected module (like `cabana-relationships.ts`) so it joins this tier and stays testable without a DB.
2. **Injected-dependency modules — tested, not gated.** `stream-actions`, `stream-cloudflare.server`, `stream-webhook.server`, `stream-tus-client`, `use-stream-upload`. These touch Supabase/network/transport, so they are exercised with their deps injected but deliberately stay out of `coverage.include` (they would drag the gate toward mock-shaped coverage).
3. **React components — rendered via SSR.** ⚠️ **vitest runs `environment: "node"`, and `jsdom` / `@testing-library` are NOT dependencies.** Component tests render with `renderToStaticMarkup` from `react-dom/server` (already a dep) — do not reach for testing-library or install a DOM; that is a deliberate constraint, not an oversight. Consequence: there is **no DOM and no click dispatch**, so a component test asserts only what SSR emits (markup, ARIA, which controls are present/enabled, and that no raw error text or secret leaks). The _behavior_ behind a control is covered in the pure module that decides it — e.g. the admin creator directory's `normalizeAdminCreatorsQuery` / `buildSearchFilter` / `mapAdminCreatorPage` in `cabana-admin-creators`, or `sanitizeRedirect` in `cabana-redirect` — not from the component test.

Before any handoff, the required gate is: `bun run lint`, `bun run build`, `bunx tsc --noEmit`, and `bun run test` all pass. ESLint retains some pre-existing react-refresh Fast Refresh warnings in shadcn UI files; those are expected. `bun run db:validate` requires Docker and exits non-zero with a clear message on hosts without it (e.g. this sandbox) — CI runs it on a Docker-enabled runner.

## Architecture

### Routing & SSR

- File-based **TanStack Router** under `src/routes/`. Flat dot-notation files map to nested paths (`dashboard.posts.tsx` → `/dashboard/posts`). `$username.tsx` is the public creator page at `/$username`. A handful of hand-built personal link-in-bio pages exist as dedicated static routes (`thetejeda.tsx`, `danielasanchez.tsx`, `eldondolla.tsx`, `td.tsx`) — bespoke pages, not the `$username` data-driven flow.
- **`src/routeTree.gen.ts` is generated — never edit it manually.** It regenerates on dev/build.
- `src/router.tsx` builds the router and injects a React Query `QueryClient` into router context.
- SSR entry is `src/server.ts` (not the bundled TanStack default — `vite.config.ts` redirects `tanstackStart.server.entry` to it, and `wrangler.jsonc` points `main` there). It wraps the handler to catch both thrown errors and h3-swallowed catastrophic 500s, returning a branded error page.
- `vite.config.ts` uses `@lovable.dev/vite-tanstack-config`, which **already bundles** tanstackStart, viteReact, tailwindcss, tsConfigPaths, the Cloudflare plugin, the `@` alias, and VITE\_\* env injection. Do not re-add those plugins or the app breaks with duplicates.
- **Deploy target is Vercel, not Cloudflare.** `vite.config.ts` sets `nitro.preset: "vercel"` and restores the preset's Build Output paths so `bun run build` emits `.vercel/output` (serverless function + static). Without this the wrapper defaults to `cloudflare-module` and Vercel serves a static bundle → 404. Production deploys are prebuilt (`vercel deploy --prebuilt`). `wrangler.jsonc` remains only because the bundled Cloudflare plugin reads it.
- `vite.config.ts` also pre-bundles every runtime dep via `optimizeDeps.include` — this prevents Vite's mid-navigation re-optimize (the raw-JS "Outdated Optimize Dep" overlay flash on lazy routes). When adding a runtime dependency, add it to that list.
- ESLint/Prettier ignore the generated `.vercel`, `.tanstack`, `.nitro`, and `.wrangler` output dirs (`eslint.config.js` + `.prettierignore`) — keep new build-output dirs out of lint scope or `bun run lint` explodes with thousands of errors.

### Supabase layer (`src/integrations/supabase/`)

Three distinct clients — pick by trust boundary:

- **`client.ts`** → `supabase`: browser/anon client (publishable key), used by all React Query hooks. RLS-enforced. Lazily instantiated via a Proxy.
- **`client.server.ts`** → `supabaseAdmin`: **service-role key, bypasses RLS.** Server-only; never import into client code.
- **`auth-middleware.ts`** → `requireSupabaseAuth`: TanStack `createMiddleware` that validates the `Authorization: Bearer` token for server functions and yields a per-request RLS-scoped client + `userId`.

`client.ts`, `client.server.ts`, `auth-middleware.ts`, and `types.ts` are marked auto-generated (Lovable Cloud) — avoid hand-editing.

A fourth middleware variant, **`optionalSupabaseAuth`** (`optional-auth-middleware.ts`), is used for guest-callable server actions (Posts feed, Discovery) that work without a session but upgrade to RLS-scoped access when one is present. Use it instead of `requireSupabaseAuth` for any public-readable route.

### Protected server-action tier (T2)

Real, RLS-scoped server functions live in `src/lib/account-actions.ts` (Phase 2B) and `src/lib/relationship-actions.ts` (Phase 2C). Each `createServerFn` composes two middlewares:

1. **`attachSupabaseToken`** (`auth-client-middleware.ts`, client side) reads the current session and attaches `Authorization: Bearer <token>` to the outgoing RPC.
2. **`requireSupabaseAuth`** (`auth-middleware.ts`, server side) validates it and yields a per-request `{ supabase, userId }` scoped to the caller's RLS.

So these handlers run under the caller's RLS, **never the service role**. Conventions:

- **Handlers stay thin.** Validation + behavior live in pure, repository-injected modules (`cabana-account.ts`, `cabana-relationships.ts`) that are unit-tested without a browser or DB. The action file just wires a `*Repository` (real Supabase queries / RPC calls) into those functions.
- **Do not put server-action files under any `**/server/**` path.** `createServerFn` compiles to a client-importable RPC bridge, and the `src/start.ts` import-protection plugin blocks `**/server/**` from client bundles. (`client.server.ts` / `supabaseAdmin` is the service-role exception and _is_ server-only.)
- The bearer token is attached **per server function**, not by a global middleware: every `createServerFn` in `src/lib/*-actions.ts` composes `attachSupabaseToken` (`auth-client-middleware.ts`) as the first client middleware in its chain. (There is no global `functionMiddleware` in `src/start.ts` — the earlier `auth-attacher.ts`/`attachSupabaseAuth` global-middleware approach was never wired and the dead file was removed; `attachSupabaseToken` is its strict superset and is the live mechanism.)
- **Admin server actions — defense in depth (preferred convention for newly added admin actions).** `admin-creator-actions.ts` establishes the pattern: (1) the route renders behind a client-side `AdminGate` (UX only — never the trust boundary); (2) the handler makes an **explicit server-side admin-role assertion** (`assertAdmin`) before returning any data, reading the caller's own `user_roles` row under their own RLS ("Users can view own roles") and throwing a generic error otherwise; (3) **RLS remains the final database boundary** on every row. Authority derives from `public.user_roles` through the authenticated caller — **never** from an email, Auth metadata, a client-supplied flag, or UI visibility. Use this for any new admin action, especially when the rows are public-by-policy (like `creator_profiles`), which would otherwise have no admin gate below the router. Note: **not** every existing admin action follows this yet — the older ones (e.g. `admin-finance-actions.ts`) rely primarily on RLS, which fails safely but has no second layer. This app-layer assertion is **not** a substitute for SQL-side checks in `SECURITY DEFINER` RPCs: any admin-only RPC must still gate internally with `is_current_user_admin()`.
- Client consumers wrap these via React Query hooks in `use-account.ts` / `use-relationships.ts`.

### Data access (`src/lib/cabana-store.ts`)

The live data layer. Tables: `creator_profiles`, `links`, `products`, plus `user_roles` and `reserved_handles`. Pattern:

- **Public reads:** `useCreatorByHandle(handle)` — used by the public profile page.
- **Owner reads:** `useCabana()` — current user's profile/links/products.
- **Owner writes:** `useCabanaMutations()` — profile/link/product CRUD + storage uploads, each wrapped with toast error handling and React Query invalidation of `["my-creator"]` / `["creator-by-handle"]`.
- Storage buckets `avatars`, `banners`, `products` are folder-scoped by `user_id` (RLS: `auth.uid() = foldername(name)[1]`).

### Auth & roles

- `src/lib/cabana-auth.ts`: `cabanaAuth` (signup/login/logout/password reset, plus `loginWithGoogle()` via `supabase.auth.signInWithOAuth`) + `useAuthSession()` / `useCabanaUser()` hooks built on `supabase.auth.onAuthStateChange`.
- **Google OAuth flow:** `LoginCard` shows "Continue with Google" → Supabase `/auth/v1/authorize` → back to `/auth/callback` (`src/routes/auth.callback.tsx`, noindex; waits for the session with a 10s timeout), which routes member → `/account`, brand-new user → `/onboarding`, else → `/dashboard`. Requires the Google provider enabled in Supabase Auth settings and `<origin>/auth/callback` allowlisted as a redirect URL.
- Routes `/` and `/login` both render `LoginCard` (`components/cabana/auth/LoginCard.tsx`, the black-marble/glass sign-in hero); `AuthShell` is still the scaffold for signup/forgot-password.
- `/onboarding` is a profile-first builder (Identity → Links → Look → Preview) that writes real `creator_profiles`/`links` data, including the `headline`/`accent_color`/`button_style` customization fields.
- Route protection is **client-side**: `dashboard.tsx` redirects to `/login?redirect=...` when no session. There is no server route guard. The `?redirect=` param is attacker-controllable — post-auth navigation must go through the pure sanitizer `sanitizeRedirect` in `src/lib/cabana-redirect.ts` (same-origin absolute paths only, default `/dashboard`; in the 95% coverage set) — never pass the raw param to `navigate`.
- `src/lib/cabana-roles.ts`: `useHasRole("admin"|"moderator"|"user")` reads `user_roles`, resolving from the cached session (no `getUser()` round-trip). Used to gate `/admin`.

### Components

- `src/components/ui/` — shadcn/ui (new-york style, ~46 primitives). Don't reinvent; compose these.
- `src/components/cabana/` — app feature components: `dashboard/` (DashHome, LinkManager, StoreManager, AnalyticsPage, MediaKit, ProfileEditor, SettingsPanel, Sidebar), `auth/AuthShell`, `foundation/FoundationPage`.
- `src/components/cabana/social/` — `SocialShell`, the three-column scaffold for the social surfaces (fixed left `SocialNav` · center content · contextual `SocialRightRail`; nav collapses to a bottom tab bar below `lg`, rail hides below `xl`; pass `rightRail={null}` to suppress the rail). The public creator page (`$username.tsx`), `/account`, the home feed, post detail, discovery, messages, and member notifications all render inside it — wrap new social/member-facing surfaces in it rather than inventing a new layout.
- `FoundationPage` is the shared "demo foundation / coming soon" presentation for not-yet-built subscription-platform screens. Several screens that once rendered through it are now real (posts, messages, earnings, notifications, feed, discover, dashboard home/analytics); it remains the fallback for any screen not yet built out.
- `src/components/cabana/QueryErrorState.tsx` — the shared inline error card for failed queries (glass card, AlertTriangle, optional Retry). **Convention: a failed query must never render fake business data** (`$0.00`, "No X yet") — render this instead, and keep genuine-empty copy for success-with-zero-rows only.

### Design system

Luxury dark / glass / chrome / iridescent aesthetic. Global styles and utility classes (`glass-strong`, `btn-luxury`, `btn-ghost`, `bg-iridescent`, `text-iridescent`, `shadow-luxury`, `animate-float`) live in `src/styles.css`. Tailwind v4 (CSS-config, no `tailwind.config`). Use these classes rather than introducing ad-hoc styling.

**Buttons:** `src/components/ui/button.tsx` is the single source of truth — "liquid-metal" design language. Metal finishes (`primary`/`cta`/`secondary`/`destructive`/`success`) share the `.btn-metal` base in `styles.css` and only swap `--metal-body`/`--metal-fg` CSS vars; low-emphasis finishes are `ghost`/`icon`/`outline`/`toolbar`/`nav`/`link`. Use these variants instead of hand-styling buttons. `LiquidMetalButton` (`ui/liquid-metal-button.tsx`) is the WebGL-shader hero CTA — reserved for landing/login/signup/onboarding CTAs, max one live shader app-wide, falls back to the CSS `cta` variant.

## Subscription-platform conventions (in-progress work)

The platform-evolution feature set (posts, fan subscriptions, messages, tips, payouts, moderation, analytics) is now largely **live against real tables + RLS** — see the phase list below. Money remains **demo-only** throughout (no processor, `mock_*` refs). A few frontend contract modules predate the live tables and are still in use:

- `src/lib/cabana-types.ts` — domain types (MemberProfile, CreatorPost, CreatorSubscription, Tip, Transaction, Payout, etc.) designed to map to future Supabase tables.
- `src/lib/cabana-entitlements.ts` — pure frontend content-access decision module (`canViewContent`). It mirrors the server-side `can_view_post` RLS check so components can gate UI pre-flight without a round-trip. In the 95% test coverage set.
- `src/lib/cabana-validation.ts` — shared URL normalization/validation helpers (`normalizeUrl`, `isValidHttpUrl`) used by creator editors. Pure, no dependencies.

**Now live (real tables + RLS, via migrations):**

- **Member accounts (Phase 2B):** an `account_type` enum (`'creator' | 'member'`) stamped on `public.profiles`, plus `member_profiles`. The `handle_new_user` trigger reads `raw_user_meta_data.account_type` (defaults to `creator`) and provisions a `creator_profiles` row only for creators. Logic in `cabana-account.ts` + `account-actions.ts`.
- **Social graph (Phase 2C):** `follows` and `blocks` tables with behavioral RLS, the `public_creator_profiles` / `public_member_profiles` views (safe public projections incl. `follower_count`), and `SECURITY DEFINER` RPCs `relationship_state`, `relationship_follow_creator`, `relationship_unfollow_creator` (granted to `authenticated` only). Logic in `cabana-relationships.ts` + `relationship-actions.ts`.
- **Posts & feed (Phase 3):** `posts` + `post_media` with public/followers visibility, a **private** `post-media` bucket (signed URLs via `getPostMediaUrls` after `can_view_post`), and ID-free `feed_creator_posts` (locked stubs for non-followers) / `feed_home_posts` RPCs. Logic in `cabana-posts.ts` + `post-actions.ts`; the guest-callable reads use `optionalSupabaseAuth`.
- **Engagement (Phase 3.2):** `post_comments` / `post_likes` / `post_saves` with block-aware RLS (`can_view_post` + `is_engagement_blocked`), soft-deletable comments, and `post_engagement_state` / `post_comments_list` / `post_card` RPCs. Logic in `cabana-engagement.ts` + `engagement-actions.ts`. Post detail at `/post/$postId`.
- **Creator subscriptions (Phase 4, DEMO-ONLY):** `creator_subscription_tiers` + `creator_subscriptions` (fan→creator; the existing `subscriptions` = SaaS plans is **not** renamed). `is_active_subscriber` gates `subscribers` posts in `can_view_post`/feed RPCs; SECURITY DEFINER write RPCs `subscribe_to_creator` (copies tier price, `mock_*` ref — **no real charge**) / `cancel_creator_subscription`. Logic in `cabana-subscriptions.ts` + `subscription-actions.ts`; UI under `components/cabana/subscriptions/`. `purchase` visibility still unsupported.
- **Messaging (Phase 5):** direct (1:1) `conversations` / `conversation_participants` / `messages` (soft-delete) / `message_read_receipts` with participant-scoped RLS via SECURITY DEFINER helpers (`is_conversation_participant`, `is_conversation_blocked`, …) and **Supabase Realtime** (`messages` + receipts in the `supabase_realtime` publication; delivery is RLS-filtered). RPCs: `create_direct_conversation` / `start_conversation_with_username`, `list_conversations`, `conversation_messages`, `mark_conversation_read`, `unread_message_count`. Logic in `cabana-messaging.ts` + `messaging-actions.ts` + `use-messaging.ts`; UI under `components/cabana/messaging/`; routes `/messages` + `/messages/$conversationId`. No paid messages/attachments/notifications.
- **Monetization ledger (Phase 6, DEMO-ONLY):** the internal financial ledger a future Stripe would settle into — **no processor, cards, webhooks, or real money.** Tables: `transactions` (append-only/immutable via a BEFORE UPDATE/DELETE trigger; CHECK that `creator_net = gross − fees`), `creator_balances` (cached projection, recomputed by `recalc_creator_balance`, never the source of truth), `payout_requests` + `payouts`, `tips`, `purchases`, `content_entitlements` (permanent access), plus `posts.price_cents`/`currency` to activate the `purchase` visibility tier. SECURITY DEFINER write RPCs `create_mock_purchase` / `create_mock_tip` / `request_payout` (all `mock_*` refs; fee model = 10% platform + 3% processor, mirrors `cabana-money`) and read RPC `creator_balance`; `has_content_entitlement` is wired into `can_view_post` / `feed_creator_posts` / `post_card` + a buyer posts-RLS policy. `is_current_user_admin()` wraps the (authenticated-revoked) `has_role` for admin read policies. Pure logic in `cabana-money.ts` (`evaluatePayoutEligibility`, `evaluatePurchase`, `entitlementFromPurchase`); server actions in `money-actions.ts`; hooks in `use-money.ts`; UI under `components/cabana/earnings/` (route `/dashboard/earnings`). Purchase unlock CTA in `LockedContentGate`; price authoring in `PostComposer`.
- **Notifications & activity (Phase 7, internal only):** the in-app event/outbox foundation — **no email/push provider** (no Resend/Firebase/Expo/web push). Tables: `notifications` (system-written; `dedupe_key` NOT NULL UNIQUE for idempotency; clients may only flip `read_at`), `activity_events` (canonical append-only log w/ `metadata` jsonb), `notification_preferences` (`in_app`/`email`/`push`; email & push are placeholders), `notification_outbox` (inert future delivery queue; **admin-only**, never user-readable). **Event generation is at the DB layer**: SECURITY DEFINER `emit_notification` (logs activity + idempotent notification + outbox rows per enabled channel; suppresses self/blocked) is called by AFTER INSERT triggers on `follows`/`post_likes`/`post_comments`/`post_saves`/`creator_subscriptions`/`tips`/`purchases`/`messages`/`payout_requests` — so events fire uniformly for both direct-insert and RPC write paths, atomically, with no edits to the Phase 2–6 action files. `notifications` is in the `supabase_realtime` publication (delivery RLS-filtered to the recipient). Pure logic in `cabana-notifications.ts` (`formatNotification`, `groupNotificationsByDay`, `countUnread`, `evaluatePreference`, `isOutboxEligible`, `notificationDedupeKey`, `activityLabel`, mappers); server actions in `notification-actions.ts`; hooks in `use-notifications.ts` (realtime); UI under `components/cabana/notifications/` (`/dashboard/notifications` + auth-gated `/notifications`); live unread badge in the dashboard sidebar.
- **Admin moderation & audit (Phase 8 slice 1, staff only):** the trust & operations foundation — a real moderation queue + an append-only audit trail. **No** admin finance views, payout approval, notification outbox/delivery, email/push, or member-facing report buttons in this slice (the report INSERT path exists and is RLS-correct so those wire in later without a schema change). Tables: `reports` (reporter creates/reads own; staff read + triage; polymorphic `subject_type`/`subject_id`, not FK-constrained) and `audit_logs` (append-only via a BEFORE UPDATE/DELETE `prevent_audit_mutation` trigger permitting only FK-null cascades; system/trigger-written — no client write grant). **Audit generation is at the DB layer**: an AFTER UPDATE trigger `on_report_change_audit` appends an immutable audit row on every report status/assignment change, atomically and uniformly across write paths (the Phase 7 pattern). `is_current_user_staff()` (admin OR moderator) wraps the authenticated-revoked `has_role` for the moderation RLS policies; `current_audit_actor_role()` stamps the actor role on audit rows. Reads/writes are plain RLS-scoped table access (no new RPCs): staff triage via a staff-only UPDATE policy (column-scoped to `status`/`assigned_admin_user_id`/`resolution`). Pure logic in `cabana-moderation.ts` (`validateReportInput`, `canTransitionReport`/`allowedTransitions` state machine, `mapReport`/`mapAuditLog`, queue `countReportsByStatus`/`sortReportsForQueue`, labels, `buildAuditEntry` mirroring the SQL trigger); server actions in `moderation-actions.ts` (`getReports`, `getReportDetail`, `getAuditLogs`, `createReport`, `assignReport`, `updateReportStatus`); hooks in `use-moderation.ts`; UI under `components/cabana/moderation/` behind a `StaffGate` at URL-backed subroutes `/admin/reports` + `/admin/audit` (the existing `admin.tsx` demo tabs are untouched except for nav cards in the Flagged tab). **Phase 8B (next slice): member-facing reporting.** The report creation path (`createReport` in `moderation-actions.ts`, the `validateReportInput` pure logic, and the `reports` INSERT RLS) is already implemented and correct — this slice adds the member-facing reporting UI on top of that existing backend (no schema change expected unless one is required, and only under the standard migration + RLS + behavioral-test gate).
- **Member reporting (Phase 8B):** the member-facing reporting UI over the Phase 8 backend. Additive migration `20260521000000_report_reasons.sql` appends `hate` + `sexual_content` to `report_reason` (a SAFETY category, not adult-content functionality). Reusable `components/cabana/reporting/` (`ReportButton` → `ReportDialog` → `ReportReasonSelect`) reuses `useCreateReport` + `validateReportInput`; wired into posts, comments, creator profiles, and direct messages (hidden for guests / own content). Member-_profile_ reporting is deferred (no surface currently exposes another member's id; `ReportButton` already supports `subjectType="user"`). Reason set + labels live in `cabana-moderation.ts`.
- **Admin finance & operations (Phase 8C, admin-only):** the finance back office over the Phase 6 ledger. **8C.1 (read-only, no schema change):** admin-scoped reads reuse the existing `is_current_user_admin` RLS on `transactions`/`payouts`/`creator_balances`; pure `cabana-finance.ts` (`summarizeRevenue`, `rollupPayouts`, `filterTransactions`, `transactionsToCsv`, labels) + `admin-finance-actions.ts` + UI under `components/cabana/admin-finance/` behind an `AdminGate` (admin-only, vs `StaffGate`); routes `/admin/finance`, `/admin/ledger`, `/admin/ledger/$transactionId` (CSV export). **8C.2 (payout workflow):** additive migration `20260522000000_admin_payouts.sql` adds one enum value `payout_request_status.on_hold`, an AFTER UPDATE trigger `on_payout_request_change_audit` that appends to the **existing** `audit_logs` (target_type `payout_request`; no second audit system), and the SECURITY DEFINER admin-gated, transition-validated `admin_review_payout(_payout_request_id, _action, _note)` RPC. The pure state machine in `cabana-payouts.ts` (`applyPayoutAction`/`canApplyPayoutAction`/queue helpers) is mirrored verbatim by the RPC; server actions in `admin-payout-actions.ts`; route `/admin/payouts`. Actions: `approve` (→approved), `reject` (→rejected), `hold` (→on_hold), `release` (→requested), `mark_paid` (→paid). **`approve` and `mark_paid` are intentionally distinct steps — `approve` AUTHORIZES (the linked disbursement stays `processing`/reserved); `mark_paid` SETTLES (disbursement → `paid`, books paid-out). Do not collapse them.** A hold keeps the payout reserved, so no `payout_status` or `recalc_creator_balance` change was needed. Every decision recomputes the balance and writes an audit row.
- **Notification engine (Phase 9A, backend only):** activates the previously-inert Phase 7 `notification_outbox`. **No email/push/SMS providers** (those are Phase 9C). Additive migration `20260523000000_notification_engine.sql` adds ONE function — `process_notification_outbox(_batch_size, _max_attempts, _result)` — and **no table/column/enum/RLS change** (the outbox already has `attempts`/`last_error`/`scheduled_for`/`processed_at` + the `(status, scheduled_for)` index). The SECURITY DEFINER, admin-gated RPC claims a batch of due `pending` rows with `FOR UPDATE SKIP LOCKED` (worker-safe, no double-delivery), applies the outcome, and returns a jsonb summary. Since there is no transport yet, `_result` SIMULATES the delivery outcome (`delivered` → `sent`; `transient_failure` → retry with exponential backoff until the attempt cap, then dead-letter; `permanent_failure` → immediate dead-letter `failed`) — 9C will replace the simulation with real per-channel provider calls. Pure logic in `cabana-notification-engine.ts` (`resolveOutboxOutcome`, `computeBackoffSeconds`/`nextRetryAt`, `selectDueBatch`, `summarizeOutbox`) is mirrored verbatim by the RPC; server actions `processOutbox` + `getOutboxStats` in `notification-engine-actions.ts`. No UI/hooks this phase.
- **Notification center UI (Phase 9B, no schema change):** the member/creator-facing surface over the Phase 7 notifications + Phase 9A engine — **frontend + pure-layer only, no new SQL** (`notification_preferences` stays per-channel/one-row-per-user; per-type granularity would need a migration and is deferred). UI under `components/cabana/notifications/` (`NotificationsCenter`, `NotificationsDashboard`, `ActivityFeed`, `NotificationSettings`, `NotificationBadge`, `MemberNotificationsPage`) consuming the `use-notifications.ts` realtime hooks (per-instance channel topics — never share a topic across hook instances); `/dashboard/notifications` is the gated center, `/notifications` is auth-gated for members (guest → foundation) and includes the preferences card. The center has day grouping, **server-side unread/type filters + the H-08 limit clamp validated by the pure `buildNotificationsListQuery`**, load-more to 200, click-through that resolves via `resolveNotificationTarget` **and marks the notification read**, and `QueryErrorState`/`EmptyState` states. **Mark-all-read is a single recipient-scoped UPDATE** built by the pure `buildMarkAllReadCommand` (unit-tested scoping — preserve it). A disabled `in_app` preference (via `evaluatePreference`, shared hook `useInAppNotificationsEnabled`) suppresses the center **and every unread badge**; email/push toggles persist but are labeled as taking effect at Phase 9C delivery launch — never present them as live delivery.
- **Discovery & search (Phase 10, public surface):** **`/discover` is now a real discovery + global search surface (no longer a placeholder)** — but it is **`noindex, nofollow`** and exposes only public projections. **10A (foundation):** pure `cabana-discovery.ts` (deterministic ranking/matching/shaping — featured/trending/recently-active/suggested creators + ranked post candidates, all from the `public_creator_profiles` view and ID-free feed data); server actions `getDiscoverySnapshot` / `getDiscoverySearchResults` in `discovery-actions.ts` use **`optionalSupabaseAuth`** (guest-callable, RLS-filtered); hooks in `use-discovery.ts`; UI under `components/cabana/discovery/` (`DiscoveryPage`). **10B (advanced ranking):** richer scoring inside the same pure module — no schema change. Nothing private is exposed on this public route.
- **Creator dashboard home (Phase 11A, no schema change):** the signed-in creator's home view model — **pure aggregation only, no new SQL.** `cabana-dashboard.ts` turns already-fetched RLS-scoped data (the `creator_balance` RPC, owned `transactions`/`payouts`/`creator_subscriptions`/`notifications`, the Phase 4 `creator_subscribers_list` RPC) into KPI cards, a revenue summary, a subscriber summary, and recent activity — **reusing** `cabana-money` (balance), `cabana-finance` (tx labels), and `cabana-notifications` (activity rows); it never recomputes fees/balances. Server actions in `dashboard-actions.ts` (`requireSupabaseAuth`); hooks in `use-dashboard.ts`; UI under `components/cabana/dashboard/overview/` (`CreatorDashboard`, `KpiCards`, `RevenueSummary`, `SubscriberSummary`, `RecentActivity`, `QuickActions`). **The creator business home is now the `/dashboard` index** (`dashboard.index.tsx` renders `CreatorDashboard`); `/dashboard/home` is a redirect to it that preserves old deep links, and the legacy link-in-bio `DashHome` moved to `/dashboard/link-in-bio` (sidebar "My Page"). A one-time `WelcomeLive` post-onboarding banner (`components/cabana/dashboard/WelcomeLive.tsx`, gated on the `cabana:justOnboarded` sessionStorage flag that `/onboarding` sets on completion) renders above the dashboard once. The subscriber roster (`SubscribersDashboard`, under `components/cabana/subscriptions/`) is at `/dashboard/subscribers`. DEMO-ONLY money throughout.
- **Creator analytics (Phase 11B):** revenue-over-time, subscriber growth, top content, and an engagement summary, filterable by date range. Additive migration `20260524000000_creator_analytics.sql` adds **ONE** SECURITY DEFINER read RPC `creator_content_analytics` (caller's own posts only, joined on `creator_profiles.user_id = auth.uid()`; returns aggregate like/comment/save **counts** only, never who liked/saved — privacy-consistent with `post_engagement_state`; granted to `authenticated`, revoked from `public`/`anon`) — **no table/column/enum/RLS/trigger change** (likes/saves are private under RLS, so a creator cannot aggregate them through base tables; this RPC fills exactly that gap). Pure `cabana-creator-analytics.ts` (named to avoid colliding with the existing link-in-bio `cabana-analytics.ts`) buckets already-derived `creator_net_cents` by time in **UTC**; it does not re-derive fees/balances. Server actions in `analytics-actions.ts` (`requireSupabaseAuth`); hooks in `use-analytics.ts`; UI under `components/cabana/dashboard/analytics/` (`AnalyticsDashboard`, `RevenueAnalytics`, `SubscriberAnalytics`, `ContentAnalytics`, `EngagementSummary`, `DateRangeFilter`) at route `/dashboard/performance` — the legacy link-in-bio `/dashboard/analytics` (page-view/click analytics) is untouched and distinct. DEMO-ONLY money throughout.
- **Audience insights (Phase 11C Option B — DB layer landed, frontend pending):** creator-only audience view, planned as a tab within `/dashboard/performance`. Additive migration `20260531000000_audience_insights.sql` adds **ONE** creator-scoped SECURITY DEFINER read RPC `creator_audience_insights(_supporter_limit, _window_days)` returning jsonb — count-only engaged-follower/recency stats (7/30/90-day windows; like/save identities are never returned) plus **named** top supporters (identity shape mirrors `creator_subscribers_list`; null-payer spend collapses into one unnamed "Former member" row), with spend summed from the caller's own succeeded `transactions` via `creator_net_cents` (fees never re-derived). No table/column/enum/RLS/trigger change; authenticated-only. Behavioral test: `supabase/tests/audience_insights.sql`. The pure module (`cabana-audience.ts`) + server actions + hook + performance-tab UI are the next 11C slice and have **not started**. The same session fixed tech-debt #1: `useCreatorByHandle` (`cabana-store.ts`) now selects an explicit column list so auth `user_id` never reaches the public wire — do not regress it to `select("*")`, and note that swapping to `public_creator_profiles` does NOT work (the view lacks the `id`/theme/customization columns the public page needs).

- **Cloudflare Stream video (SHIPPED to production July 16 2026 — PR #24 squash-merged to `main` as `d2b7137`, production deploy `dpl_JDWXsAD5…` READY on cabanagrp.com; pure layer, DB contract, server actions, live webhook, upload transport/hook, composer UI (5A.3), publish readiness, playback (5B), detach (5A.4), and orphan reconciliation all landed; the real-video lifecycle was verified end-to-end against the production Cloudflare account + live webhook before merge — see the July 16 handoff):** video upload/playback via Cloudflare Stream, ALWAYS-SIGNED playback (public posts included). Pure domain + wire parsing in `cabana-stream.ts` (upload policy 1 GiB/600 s/3 concurrent/20-day, status machine with terminal ready/error, media-mix one-video-XOR-images, storage-path convention `<owner>/stream/<uid>`, tus metadata/webhook-signature/envelope parsing, playback-batch orchestration — all I/O injected; in the 95% coverage set). DB contract in migration `20260536` (see chain below; applied to cloud July 12). Server actions in `stream-actions.ts` (`createStreamUploadTicket`/`attachStreamVideoToPost`/`getStreamVideoStatus`/`getStreamPlayback(+Batch)`/`deleteStreamVideo` — caller-RLS writes, `can_view_post` before any service-role read, reject-when-attached delete model); the ONLY Cloudflare-API-secret-bearing module is `stream-cloudflare.server.ts` (env loader naming `CLOUDFLARE_ACCOUNT_ID`/`CLOUDFLARE_STREAM_TOKEN`/`CLOUDFLARE_STREAM_CUSTOMER_SUBDOMAIN`; `STREAM_SIGNING_KEY_ID` reserved-unused; fetch with timeouts; strict envelope parsing). **Webhook (Checkpoint 4):** `POST /api/webhooks/stream` (`src/routes/api.webhooks.stream.ts`, the repo's first server route — POST-only, no component, handler dynamically imports the server module) → `stream-webhook.server.ts` (the ONLY reader of `CLOUDFLARE_STREAM_WEBHOOK_SECRET`; HMAC-SHA256 over `<literal time>.<raw body>` via the pure `verifyStreamWebhook`, constant-time + 300 s window; strict payload parsing rejects live-input states; lifecycle applied by REUSING `executeStatusRefreshFlow` with the webhook body as the snapshot — one shared compare-and-set, terminal ready/error never regress, `post_media` syncs `processing_status`/`width`/`height` only; unknown UID → 200 no-op; DB failure → 500 for Cloudflare retry; bodies never carry secrets/internals). **The webhook is LIVE in production since July 13 2026**: registered with Cloudflare at `https://cabanagrp.com/api/webhooks/stream`, with `CLOUDFLARE_STREAM_WEBHOOK_SECRET` and `CLOUDFLARE_STREAM_CUSTOMER_SUBDOMAIN` set in Vercel Production + Preview. All four Stream env vars in Vercel are **sensitive type — write-only, never readable back** via CLI/API; rotation always means re-supplying values (local gitignored `.env` holds working copies). The webhook is the primary lifecycle driver; owner polling is the bounded fallback. **Upload transport (Checkpoints 5A.1 + 5A.2, July 14 2026):** the pure upload-session state machine is `cabana-stream-upload.ts` (event-driven `transitionUploadSession` with typed denials, retry categories + capped exponential backoff, `preflightUploadFile`, `requiredCleanupSteps`, tus chunk-size selection; in the 95% coverage set); `stream-tus-client.ts` is the **ONLY** module importing `tus-js-client` (`retryDelays: null` — retry policy belongs to the state machine, never tus; offset-preserving `abort(false)` pause; errors reduced to safe codes only); `use-stream-upload.ts` is the framework-free `createStreamUploadController` (ALL deps injected: server actions, transport factory, timers) plus the thin `useStreamUpload` React hook (`session`, `beginUpload(file, postId)`, `pause`/`resume`/`retry`/`cancel`/`reset`, derived flags incl. `cleanupRequired`/`detachRequired`, `preflightRejection`). `tus-js-client` is in both lockfiles and vite `optimizeDeps`. **Composer UI (5A.3):** pure policy in `cabana-composer-media.ts` (one-video-XOR-images, per-phase controls, publish/draft gates + reasons, unload guard, safe copy keyed to stable codes — no upstream error text can leak; in the 95% coverage set) projected by `VideoUploadCard`/`PostComposer`.

  **Publish readiness (5A.4):** `publishPost` (post-actions.ts) now calls `assertPublishableMediaRows` BEFORE the update, reading the caller's own rows under RLS. `resolveMediaProcessingStatus` (cabana-stream) judges a Stream row by **`stream_videos.status`, never `post_media.processing_status`** — the latter is written best-effort and can lag, so trusting it would make skew permanently publish-blocking; a Stream row with no lifecycle row fails closed; images keep their own status (default ready) and are unaffected. **Raw-PostgREST hardening (migration `20260541`, applied to cloud — verified July 16 2026):** the raw-PATCH-to-published bypass and the forged-`status='ready'` insert are now closed database-side. `20260541000000_stream_publish_integrity.sql` adds (A) a `BEFORE INSERT OR UPDATE` trigger on `posts` (`assert_post_media_publishable`, SECURITY DEFINER, fires only on the transition INTO published) that rejects publishing when any attached cloudflare-stream media lacks a genuinely READY `stream_videos` row — judged by `stream_videos.status`, fail-closed on a null/missing lifecycle row, image-only posts unaffected; and (B) narrows the `authenticated` INSERT grant on `stream_videos` from table-wide to EXACTLY the four `insertTicketRow` columns, so every provider-controlled column (`status`/`ready_at`/dimensions/error) is non-insertable (42501) — the webhook (service_role) stays the only writer that advances status, and there is no authenticated UPDATE grant. Behavioral test `supabase/tests/stream_publish_integrity.sql`; `stream_videos.sql` (21b) updated to the column-scoped model (`has_table_privilege` does not roll up column grants, so table-wide INSERT is now correctly false). Cloud is at `20260541` (verified July 16 2026 via the MCP/Management-API connection): the trigger `posts_publish_media_ready` is live and the `stream_videos` authenticated INSERT grant is narrowed to exactly `uid, owner_user_id, creator_profile_id, upload_expires_at` (authenticated lifecycle UPDATE denied; service_role only). This corrects the earlier on-branch note — the cloud already carried these objects when the July 16 verification session began; no SQL was applied by that session.

  **Playback (5B):** `useStreamPlayback` (`use-stream-playback.ts`) is the twin of `usePostMediaUrls` — same cache-key shape, same FeedBatchScope gate, staleTime 10 min under the 1-hour token TTL. `PostMediaGallery` forks on `hasVideo`; since `PostCard` is the single shared renderer, feed + `$username` posts + discovery + post detail all get playback at once; the `$username` Media tab posters video from the signed thumbnail. `StreamVideoPlayer` is **poster-first, click-to-mount iframe** — Cloudflare serves HLS which plays natively only in Safari, so the alternative was hls.js (~150KB, both lockfiles, owning ABR/SSR-safety); the iframe is already built by `buildStreamPlaybackUrls`. Nothing autoplays (`autoplay=true` rides only the post-click mount). `FeedBatchScope` gained a third effect for playback, kept separate from media because the TTLs differ. Tokens are fetched via React Query (never a route loader) so none land in SSR'd HTML.

  **Detach (5A.4):** `detachStreamVideoFromPost` drops the `post_media` row FIRST, then reclaims the asset — the reverse would leave a post pointing at a destroyed video. The controller's `removeAttached` only clears the session on **confirmed** detach; a failure leaves the card showing the still-attached video rather than lying. `canRemove` (local discard) and `canDetach` (server round-trip) are separate flags because one cannot fail and the other can.

  **Reclamation + reconciliation:** `stream-reclaim.server.ts` deletes Cloudflare FIRST and keeps the row on failure — the row is the only record the asset exists, so keeping it is what makes retry possible; `not_found` counts as success (idempotent). `deletePost` now reclaims inline (it previously orphaned assets forever). `stream-reconcile-actions.ts` + `/admin/stream` is the sweep: `assertAdmin` under caller RLS for AUTHORITY, service-role for the WORK (`stream_videos` is owner-only SELECT by design, so a caller-scoped sweep would only ever see the admin's own uploads), bounded ≤200 rows, **dryRun defaults TRUE**, and it **never enumerates the Cloudflare account** — candidates come only from `stream_videos` rows, so an asset with no local record is left alone rather than guessed at. Retry needs no attempt counter: a failed row keeps its row and resurfaces next report. `DEFAULT_ORPHAN_GRACE.readyUnattachedMs` is **7 days** (was 24h — a day strands anyone who uploads Friday and composes Monday).

  **Client bundle is verified free of every Cloudflare/service-role marker**; modules reaching secrets (`stream-reclaim.server`) are imported **dynamically** inside handlers of client-importable RPC bridges (`post-actions`, `stream-actions`, `stream-reconcile-actions`) — the webhook route's convention. **Real-video verification DONE (July 16 2026):** with explicit owner approval for one throwaway asset, a real 2s 320×240 synthetic clip was taken through the full lifecycle against the production Cloudflare account + the live webhook — real tus upload, webhook-driven `pending_upload → ready`, publish gate (server action + the `20260541` DB trigger + forged-`ready`-insert denial all proven live), signed playback 200 / unsigned 401, app-path delete, and **independent** DB (MCP) + Cloudflare (direct API) residue checks both clean (asset 404, zero rows). **Merged + deployed July 16 2026** (PR #24 squash-merged as `d2b7137`; production deploy READY on cabanagrp.com). There is still only ONE Cloudflare account (it backs the live production webhook), so any further real-video exercise touches production (see `CABANA_TECH_DEBT.md`).

- **Admin creator pages (Phase 2A — SHIPPED to production July 15 2026; PR #25 merged as `15cb8ad`, cloud schema through `20260540`):** staff-operated management of creator link-in-bio pages (`creator_profiles` + `links`) with a draft/published/archived lifecycle, plus audited staff-role management. The public `$username` page now honors `page_status` and renders through the extracted, pure-view-model directory `components/cabana/creator-page/` (`CreatorPageSurface`/`CreatorPageLinks`; model + row mapping in `cabana-creator-page-view.ts`). **Six new pure modules join the 95% gate** (`vitest.config.ts` is the source of truth): `cabana-admin-creators` (directory query normalization + PostgREST-safe search + row→view-model; `user_id` and email NEVER cross the wire — `claimed` is a server-derived boolean; reads paginate via `rangeForPage`, never cap), `cabana-creator-pages` (page-status transition machine, appearance/link allow-lists, reorder validation, error mapping — **mirrors the `20260538` RPCs; the SQL is canonical**), `cabana-admin-roles` (staff-role mutation input validation), `cabana-admin-creator-editor` (identity/appearance/link draft validation), `cabana-admin-creator-page-detail` (editor read-model + row→wire mapping — no email / `auth.users` / audit network metadata), and `cabana-creator-page-view`. Thin **caller-RLS-scoped** server actions (never service-role): `admin-creator-actions.ts` (read-only directory; the older read slice), `admin-creator-page-actions.ts` (the eight mutations over the `20260538` RPCs), `admin-creator-page-read-actions.ts` (editor detail + audit-history reads), `admin-role-actions.ts` (audited role grant/remove over `20260539`) — every one composes `attachSupabaseToken` + `requireSupabaseAuth` + the in-handler `assertAdmin` second layer (defense in depth; the DB RPCs re-check admin and RLS remains the final boundary). Hooks: `use-admin-creators.ts` (`keepPreviousData` paging), `use-admin-creator-page.ts` (editor reads), `use-admin-creator-page-mutations.ts`. UI under `components/cabana/admin-creators/` behind `AdminGate` at routes `/admin/creators` (directory), `/admin/creators/new` (create form), `/admin/creators/$creatorProfileId` (editor: identity / appearance / link manager / lifecycle / ownership / preview / audit panel). `types.ts` is current for `20260537`–`40` (regenerated from cloud after the July 15 release; the cast shims are gone). No invite/claim flow yet: admin-created pages are ownerless (`user_id` null) drafts until a later claim slice.

Migrations are ordered and rebuild from zero: `20260511000000_baseline.sql` (profiles, creator_profiles, links, products, analytics_events, subscriptions, user_roles, reserved_handles) → `20260512000000_member_accounts.sql` → `20260513000000_social_relationships.sql` → `20260514000000_posts_feed.sql` → `20260515000000_engagement.sql` → `20260516000000_creator_subscriptions.sql` → `20260517000000_messaging.sql` → `20260518000000_monetization_ledger.sql` → `20260519000000_notifications_activity.sql` → `20260520000000_admin_moderation.sql` → `20260521000000_report_reasons.sql` → `20260522000000_admin_payouts.sql` → `20260523000000_notification_engine.sql` → `20260524000000_creator_analytics.sql` → `20260525000000_baseline_grants.sql` (corrective: table-level GRANTs for the seven baseline tables, which had RLS policies but no PostgREST role privileges — mirror each table's policy intent, change no policies) → `20260526000000_user_roles_admin_policy.sql` (routes `user_roles` admin policies through `is_current_user_admin()`) → `20260527000000_profiles_select_grant.sql` (corrective: `grant select on public.profiles to authenticated` — `profiles` had an own-row SELECT policy but no table grant, so `useAccountType` 403'd and the dashboard guard hung; `baseline_grants` had missed `profiles`) → `20260528000000_profile_customization.sql` (additive: `creator_profiles.headline` + `accent_color` (hex CHECK) + `button_style` (`rounded`|`pill`|`square` CHECK), all defaulted so older profiles keep working) → `20260529000000_post_media_service_grant.sql` (corrective: `grant select on public.post_media to service_role` — hosted Supabase grants it via platform ACLs but from-zero rebuilds don't, so `getPostMediaUrls` failed with `42501` and post images never rendered) → `20260530000000_high_qa_fixes.sql` (corrective, additive-only — no new tables/columns/enums/RLS: H5 makes `public_creator_profiles.post_count` a real count of published posts instead of a hardcoded `0`; H8 adds a per-`(buyer, post)` transaction-scoped advisory lock to `create_mock_purchase` so concurrent unlocks can't double-charge; H9 adds a per-creator advisory lock to `request_payout` so concurrent requests can't over-reserve `available_cents` negative) → `20260531000000_audience_insights.sql` (Phase 11C Option B, additive — ONE creator-scoped SECURITY DEFINER read RPC `creator_audience_insights(_supporter_limit, _window_days)` returning jsonb: count-only engaged-follower/recency stats plus named top supporters summed from the caller's own succeeded `transactions`; no table/column/enum/RLS/trigger change; authenticated-only) → `20260532000000_creator_profiles_anon_column_grant.sql` (corrective, additive-only — no table/column/enum/RLS-policy/data change: the baseline `Public can view creator profiles` policy is `using (true)` and `20260525` grants `anon` a **table-wide** SELECT, so anon could read `creator_profiles.user_id` (the account's auth UUID) straight off the base table via PostgREST; this revokes the anon table-wide SELECT and re-grants anon a **column-scoped** SELECT on the 13 public columns only — `user_id` omitted. The public policy is unchanged and `authenticated` keeps full-table SELECT so owner reads of `user_id` still work) → `20260533000000_post_media_ownership_check.sql` (corrective, additive-only — no table/column/enum/grant/data change: the baseline `post_media` write policy's WITH CHECK validated only `owner_user_id = auth.uid()`, so any authenticated creator could `POST /rest/v1/post_media` a row referencing **another creator's** `post_id` (posts are publicly/follower-readable) with an arbitrary `storage_path` — injecting media into a victim's post, which the feed RPCs aggregate by `post_id` and the service-role signer serves. This drops+recreates the policy with USING unchanged (owner-only SELECT/UPDATE/DELETE) and a tightened WITH CHECK additionally requiring the caller to OWN the target post (`is_current_user_creator` on `posts.creator_profile_id`) and `storage_path`'s first segment to equal the caller's uid; the app-layer `addPostMedia` guard was bypassable via raw PostgREST, so the fix is at the DB) → `20260534000000_recalc_creator_balance_internal_only.sql` (corrective, additive-only — no table/column/enum/RLS/data change and no function-body change: `recalc_creator_balance` (Phase 6) was granted EXECUTE to `authenticated` but is only ever invoked via `PERFORM` inside SECURITY DEFINER RPCs (which run as the definer/owner and need no caller grant), so any signed-in user could `POST /rest/v1/rpc/recalc_creator_balance` for an arbitrary `creator_profile_id`/`currency` — a cross-user balance recompute plus permanent junk `creator_balances` rows for arbitrary currency strings the owner can't remove. This revokes EXECUTE from `public`/`anon`/`authenticated` (internal-only, mirroring `emit_notification`); the function keeps `search_path=''` and all internal callers/admin-payout/purchase/tip/refund flows still recompute balances) → `20260535000000_analytics_events_hardening.sql` (corrective/additive — no RLS-policy/other-table change: `analytics_events` accepts anon inserts for any real creator profile (the intended link-in-bio tracking path) but the columns were unconstrained, so any publishable-key holder could insert arbitrary `event_type` strings and unbounded `metadata`/`target_id` (schema pollution + multi-MB storage spam) and could set `id`/`created_at` explicitly (event backdating that skews the time-bucketed dashboards), and the owner-analytics reads (`profile_id` + `created_at` range/order) had no index. Adds a NOT-VALID CHECK constraining `event_type` to the app's `CabanaEventType` set (`page_view`|`link_click`|`product_click`), a NOT-VALID CHECK requiring `metadata` to be a JSON object ≤ 4 KB and `target_id` ≤ 256 chars, the `(profile_id, created_at desc)` index, and **narrows the anon/authenticated INSERT grant from table-wide to the four intended columns** (`profile_id`, `event_type`, `target_id`, `metadata`) so `id`/`created_at` are always the column defaults. NOT VALID so a later cloud apply can't fail on pre-fix historical rows; RLS is orthogonal to column privileges so both INSERT/SELECT policies + the authenticated SELECT grant are unchanged; invalid profile targets were already blocked by the FK + INSERT policy. `smoke.sql`'s grant assertion updated to check the column-scoped privilege) → `20260536000000_stream_videos.sql` (Cloudflare Stream DB contract, additive — the ownership + lifecycle model for video, NO Cloudflare API/app code: `stream_video_status` enum; `stream_videos` upload ledger (a row exists from ticket issuance so orphaned Cloudflare assets stay sweepable; owner-scoped RLS SELECT/INSERT/DELETE with `is_current_user_creator` on INSERT; **no client UPDATE path** — status flips are system writes via explicit service_role select/update/delete grants; `unique (id, owner_user_id)`); `post_media.stream_video_id` (nullable) with a **composite ownership FK** `(stream_video_id, owner_user_id) → stream_videos (id, owner_user_id)` ON DELETE CASCADE — combined with the untouched 20260533 WITH CHECK this makes attaching another creator's stream video declaratively impossible; a coherence CHECK ties bucket `'cloudflare-stream'` to the FK; a partial unique index caps each stream video at ONE live attachment at a time; column-scoped `grant update (processing_status, width, height) on post_media to service_role` for the future lifecycle writer. Stream rows use `storage_path '<owner_user_id>/stream/<cf_uid>'` (satisfies the 20260533 path check by construction) and feed RPCs aggregate them unchanged. Deleting a post leaves the `stream_videos` row unattached + sweepable; deleting a `stream_videos` row cascades its media row) → `20260537000000_creator_page_visibility.sql` (Phase 2A.1, schema+RLS+view only — no RPC/trigger/SECURITY DEFINER: a `page_status` lifecycle enum (`draft`|`published`|`archived`, **default `published`** so every existing row + self-signup creator stays visible), appearance fields `font_family`/`background_style` (CHECK) on creator pages, link `kind` + per-link `is_visible`, and an HTTP/HTTPS scheme-prefix guard on `links.url` (`links_url_http_scheme`, NOT VALID). Base-table RLS enforces "anon sees published only; owners see their own page in any state; admins see all", links inherit their parent page's visibility, and `public_creator_profiles` is filtered to published rows — the public `$username` page reads the base table directly so the boundary lives in both places. RLS gotcha resolved: a `USING` clause needs the role's column-SELECT on referenced columns (anon got a column-scoped `page_status` grant) and a cross-table subquery needs table-level SELECT (the baseline "Owners manage own links" policy was rescoped to `authenticated`)) → `20260538000000_admin_creator_page_management.sql` (Phase 2A.2, additive — the admin WRITE foundation over 2A.1: **eight admin-only SECURITY DEFINER RPCs** (create/update/set-status/transfer page; upsert/set-visibility/reorder/delete link) + internal `write_creator_audit`. No visibility-policy change, no invite/claim, no service-role dependency. Every RPC: `search_path=''`, rejects unauthenticated, INTERNAL `is_current_user_admin()` gate (authority from `user_roles`, never an email/flag), trusted-row targets locked `FOR UPDATE`, generic non-leaking errors, revoked from public/anon + granted to authenticated. Each successful mutation writes exactly ONE `audit_logs` row in the same transaction (target types `creator_profile`/`creator_link`); owner Auth UUIDs recorded only on transfer) → `20260539000000_audit_log_visibility.sql` (Phase 2A.3, corrective+additive — closes two control-plane gaps: (1) moderators inherited admins' full audit visibility, so a new **exact** `is_current_user_moderator()` helper narrows moderator audit SELECT to an operational report action/target allow-list (finance/ownership/role rows become admin-only); (2) admins could mutate `user_roles` directly via PostgREST with no audit, so role changes move behind two SECURITY DEFINER RPCs (grant/remove staff role — direct authenticated DML on `user_roles` revoked), auth derived from `auth.uid()`+`user_roles`, serialized, each writing one audit row (`user_role.granted`/`user_role.removed`)) → `20260540000000_creator_page_integrity.sql` (Phase 2A.4, additive+corrective — DB invariants now authoritative for every client: a **partial unique index = one creator page per non-null `user_id`** (null owners, i.e. admin-created drafts, excluded; a DO-block fails loudly rather than auto-repairing pre-existing duplicates), owner-editable column ACLs that EXCLUDE lifecycle (`page_status`) and ownership (`user_id`) so an owner can't self-publish/transfer, immutable link parentage, race-safe admin transfer, and a bounded admin activity read that continues to honor audit-log RLS) → `20260541000000_stream_publish_integrity.sql` (Stream hardening, additive — no table/column/enum/policy/data change; **applied to cloud, verified July 16 2026**: (A) a `BEFORE INSERT OR UPDATE` trigger on `posts` rejects publishing a post whose attached cloudflare-stream media is not backed by a genuinely READY `stream_videos` row — closes the raw-PostgREST publish bypass left by the table-wide `posts` UPDATE grant; (B) narrows the `authenticated` `stream_videos` INSERT grant to the four ticket columns so `status='ready'`/`ready_at`/dimensions/error can't be forged at insert. Test `stream_publish_integrity.sql`; `stream_videos.sql` 21b updated to the column-scoped grant model). Every migration from Phase 3 onward grants table privileges explicitly — new tables need both RLS policies **and** GRANTs or anon/authenticated calls fail with `42501` before any policy runs. Behavioral tests live in `supabase/tests/` (`smoke.sql`, `member_accounts.sql`, `social_relationships.sql`, `posts_feed.sql`, `engagement.sql`, `creator_subscriptions.sql`, `messaging.sql`, `monetization_ledger.sql`, `notifications.sql`, `admin_moderation.sql`, `admin_payouts.sql`, `notification_engine.sql`, `creator_analytics.sql`, `user_roles_policy.sql`, `profile_customization.sql`, `post_media_service_grant.sql`, `high_qa_fixes.sql`, `audience_insights.sql`, `creator_profiles_anon_grant.sql`, `post_media_ownership.sql`, `recalc_creator_balance_grant.sql`, `analytics_events_hardening.sql`, `stream_videos.sql`, `creator_page_visibility.sql`, `admin_creator_page_management.sql`, `audit_log_visibility.sql`, `creator_page_integrity.sql`, `stream_publish_integrity.sql`); `supabase/seed.sql` seeds local data (incl. an `aurora` demo `purchase` post, two demo `reports` from a demo member, and two demo `payout_requests`). The pure-layer coverage set (95% gate) is the list enumerated under **Tests** above — keep new domain logic in a repository-injected pure module so it joins that set.

Hard constraints when working here:

- Do **not** treat mock transactions as real money; label all monetization as demo-only. Use **integer cents** for mock money.
- Do **not** present demo/sample/fake UI as real (Batch 1 of the July 2026 UI/UX audit purged this — keep it purged). Label sample surfaces with the amber demo pill (`inline-flex rounded-full border border-amber-400/30 bg-amber-400/10 px-3 py-1 text-[11px] font-medium text-amber-200/90`, as used by FinanceShell/MediaKit/admin); disable handler-less controls with a `title` explaining why; never fall back to the `aurora` demo identity on user-facing surfaces.
- Do **not** add Stripe/payments, real payouts, KYC, or adult-content functionality.
- Do **not** put private member/message data on public routes. `/discover` is a real **public** discovery + search surface (Phase 10) but is `noindex` and serves only public projections via the guest-callable `optionalSupabaseAuth` RPCs — keep it that way (never surface private/owner data there). `/messages`, `/feed`, `/post/$postId` are real and enforce visibility server-side (RLS + feed RPCs / `can_view_post`). `/notifications` is public but **auth-gated on the client**: guests see a foundation, signed-in members get RLS-scoped data (`/dashboard/notifications` is the gated creator/member center). Financial tables and notification tables (`notifications`, `activity_events`, `notification_preferences`, `notification_outbox`) grant **no anon access**; `notification_outbox` is **admin-only**. Notifications are system-written via SECURITY DEFINER triggers (`emit_notification`) — clients may only flip `read_at` (column-scoped grant).
- Treat the `transactions` ledger as **append-only**: never update historical money; record reversals as new `refund` rows. The immutability trigger permits only FK-null cascades (so accounts can be deleted while ledger rows are retained). Likewise `activity_events` is an append-only canonical log.
- Do **not** add new tables, write new migrations, or rename existing tables without an approved migration + RLS design + behavioral tests (the Phase 2B–8 tables landed under exactly that gate; the rest of Phase 8 and phases beyond it are gated and must not start automatically). `audit_logs` is **append-only** (system/trigger-written; no client write grant) — never edit/delete audit rows. Note: existing `subscriptions` = CABANA SaaS plans; fan-to-creator subscriptions must use `creator_subscriptions`.
- Keep domain logic Supabase-ready with RLS-ready ownership models; keep changes small and reviewable.

## Post-deploy verification

**Run `bun run smoke:prod` after every production deploy** (`scripts/smoke-prod.ts`; credentials in the gitignored `.env.smoke` — template + instructions in `.env.smoke.example`; one-time member-account setup via `bun run smoke:prod --setup`). It smoke-tests production (cabanagrp.com + the cloud Supabase backend) **as real users**: anon + two password sessions on the publishable key only — it aborts if given a service-role key, never touches payouts/moderation-writes/tips/purchases, prefixes all created data `smoke_<ts>`, cleans up in `finally` blocks plus a startup sweep, and ends with a residue scan. Exit 1 only on real FAILs (a check that passes on retry is FLAKY, not FAIL).

What it covers: deploy freshness (security headers + a this-cycle route serving the app shell vs the 404 sentinel), avatar-bucket upload/download + anon-upload rejection, the public-post media path (`can_view_post` + `feed_creator_posts` + storage serving), locked-post stubs leaking zero storage URLs to anon, notification recipient scoping (including the staff-account leak fix — reads through the app's `.eq("recipient_id", …)` path), two simultaneous realtime message subscriptions on distinct per-instance topics, the Stream webhook rejecting unsigned POSTs with 401 (`STREAM-WEBHOOK` — PASSing in production since the webhook went live July 13 2026; it still SKIPs rather than FAILs if the route 404s or the Vercel secret is missing (500), and it sends no signed request), and DB state (`creator_content_analytics` denying anon but existing, real `post_count`, admin finance name joins).

What it can **not** cover: the Google OAuth leg (needs a real browser + Google account), visual/UI rendering (it never runs a browser — pair with a manual look at the deployed app), the service-role signing path itself (`getPostMediaUrls` signs with `supabaseAdmin` inside the Vercel function; the script verifies the same `can_view_post` gate + object serving via the owner-scoped signer instead), and email/push delivery (none exists yet). Known intentional residue per run: system-written notification/activity rows (clients can't delete them; the script marks its notification read) and empty conversation rows (no user-facing delete path; messages are soft-deleted).

## Environment

`.env` provides `SUPABASE_URL` / `SUPABASE_PUBLISHABLE_KEY` (server) and `VITE_SUPABASE_URL` / `VITE_SUPABASE_PUBLISHABLE_KEY` / `VITE_SUPABASE_PROJECT_ID` (client, Vite-injected). `SUPABASE_SERVICE_ROLE_KEY` is required for `supabaseAdmin` and must never reach the client. Supabase migrations live in `supabase/migrations/`.

Backend topology (as of July 2026):

- `.env` points at the **cloud** cabanadatabase project (`rpzaeqoqcaxxavltgvpe`); the local-Docker-stack values are kept commented in the same file for `db:reset`/`db:validate` work. **The deployed server function (Vercel) must carry, for production/preview: the client `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY`/`VITE_SUPABASE_PROJECT_ID` AND the non-`VITE_` server runtime vars `SUPABASE_URL` + `SUPABASE_PUBLISHABLE_KEY` (read via `process.env` by every server action) AND `SUPABASE_SERVICE_ROLE_KEY`** — the server-only admin client (`supabaseAdmin`, `client.server.ts`) signs private post-media URLs at request time and **throws if the service-role key is absent, so post images won't render in production without it**. The service-role key is RLS-bypassing: keep it server-only (no `VITE_` prefix, never imported into a client module), but it **must** be present on the server at runtime. See `.env.example` for the full required set.
- `supabase/config.toml` still names the old project id — that is local-tooling-only and deliberately untouched; do not "fix" it to the cloud project.
- `supabase/reconcile/` holds the one-time cloud reconciliation scripts, audit, and backups that migrated cabanadatabase to the CABANA schema. It is history, not part of the migration chain — new schema work goes in `supabase/migrations/` (validated on local Docker first, applied to cloud only with explicit approval).
